# Avoid `src/lib/` for app code — some global gitignore rules ignore `lib/`.
# Shared modules live under `src/platform/`.
node_modules
.next
out
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store


trainco-v1/